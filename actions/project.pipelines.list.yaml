---

name: project.pipelines.list
description: "List all project pipelines"

runner_type: python-script
entry_point: get_pipelines.py

parameters:
  url:
    type: string
  project:
    type: string
  token:
    type: string
  verify_ssl:
    type: boolean
    default: False
